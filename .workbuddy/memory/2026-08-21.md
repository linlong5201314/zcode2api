# 2026-08-21 工作日志

## zcode2api 提交 + 代码审查修复

### 提交记录（本地 master，待推送 GitHub）
- `b656961` feat: OpenAI Responses API 兼容层 + 后台双因素认证（X-Admin-User + Bearer）+ Coding Plan 额度刷新兼容 + Railway/Zeabur 部署配置（用户原有改动，36 测试全过后提交）
- `bb41810` fix: 本次审查修复

### 本次审查发现并修复的问题
1. **验证码重试 bug**：`_forward_once` 中验证码被拒后 invalidate 但重试仍用同一个失效 verify_param，3 次重试注定全败。修复：重试前重新 `get_verify_param(port)`。
2. **Responses 流式丢失工具调用**：`_responses_sse` 只转发 text_delta，忽略 tool_use 块 → Codex 流式 function calling 完全失效。修复：合成完整 function_call 输出项事件链（output_item.added / function_call_arguments.delta / done / output_item.done），completed 事件携带聚合 tool_calls。对缺 content_block_start 的异常流做文本兜底。
3. **sequence_number 重复**：原用 len(text_parts) 计算导致前几个事件序号都是 0，改独立递增计数器。
4. **404 提示缺 /v1/responses**：main.py 补全。
5. **额度刷新无节流**：每次成功对话请求都会立即打 3-5 个上游额度接口（_safe_refresh），与后台 60s 周期任务重复。加 30s 节流。

### 测试
41 passed（原 36 + 新增 5：验证码重试链路 ×2、流式 tool_use 转换、sequence_number 递增、openai 流式 tool_calls）。

### 待办
- git push 失败：环境无 GitHub 凭证（无 SSH key、无 gh CLI、Windows 凭证管理器无缓存、无 env token）。本地领先 origin/master 3 个提交，需用户提供凭证方式。
